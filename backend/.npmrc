# Dev-machine install config.
# Prisma/@prisma/client lifecycle scripts are not run (their engines + client are
# produced explicitly with `npm run generate` / `npx prisma generate`), so we can
# skip every package's install scripts and avoid npm's allow-scripts abort.
ignore-scripts=true